node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Runtime data (customer bookings, uploaded licenses, admin config) - NEVER commit
# Anchor to repo root so src/data (source files) is NOT ignored.
/data/
uploads/
